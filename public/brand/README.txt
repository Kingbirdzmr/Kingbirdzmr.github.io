Hokkaido University symbol asset
================================

Header.astro is already configured to use:

    /public/brand/hokudai-symbol.png

Please download the official Hokkaido University symbol from the University's
internal symbol/application download page and save it here as:

    hokudai-symbol.png

Until that file is added, the header automatically shows a small "HU" fallback,
so the site will still build and display normally.
